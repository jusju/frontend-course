(x, y) => x + y
// callback functions don't have a name,
// just returns a value

arrays are not immutable
const makes array immutable

scope should be as narrow as possible

this.props.firstname
how to refer to props

ES6 has been SINCE SPRING 2017!

If state needs to be updated use state,

State and props are javascript objects

in render:
if (this.state.url !== '') {
return <div>
</div>

} else {
    return <div></div>
}

missed one }
